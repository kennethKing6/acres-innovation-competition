import Button from '@mui/material/Button';
import * as React from 'react';

export default function TaskButton() {
    return (
        <Button variant="outlined">~Yes we work here</Button>
    );
  }